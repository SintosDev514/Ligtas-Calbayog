# Public environment variables (inlined into the JS bundle at build time).

# Used for the Mapbox Satellite basemap on all satellite-styled maps.
# Value is a "public" (pk.) Mapbox access token; see apps/admin-web/.env.
EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.your_mapbox_public_token